# action.yml
name: 'ns-atom-build'
description: 'build ns-atom'
inputs:
  version:
    description: 'output version'
    required: true
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.version }}
